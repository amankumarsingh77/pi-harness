node_modules
dist
.agents/
.turbo
.next
.reports
test-results/
apps/dashboard/test-results/
*.log
*.tsbuildinfo
.env
.env.local
.env.harness
.harness/
coverage
graphify-out/

# Local agent / tooling state — not project source
.claude/
.codex/
.pi/*
!.pi/
!.pi/agent/
!.pi/agent/skills/
.pi/agent/skills/*
!.pi/agent/skills/graphify/
!.pi/agent/skills/graphify/**
.playwright-mcp/
skills-lock.json

# Docs — kept local, not pushed …
docs/*
# … except spec artifacts and UI mocks, which are committed for PR review.
# (Re-include the dir first, then its contents — git won't descend into an
#  excluded dir, so each level must be un-ignored explicitly.)
!docs/spec/
!docs/spec/**
!docs/mocks/
!docs/mocks/**

# OS / editor noise
.DS_Store

# Manual screenshots used during exploration
plan-head.png
plan-page-actual.png
.worktrees/

use_default_settings: true

server:
  secret_key: "pi-harness-local-searxng-change-me"
  limiter: true
  image_proxy: true
  port: 8080
  bind_address: "0.0.0.0"

valkey:
  url: valkey://searxng-valkey:6379/0

search:
  safe_search: 1
  formats:
    - html
    - json
